import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

// Minimal sidebar for Phase 1 — just the intro. Phase 2 grows this into
// the full operator documentation tree (configuration, sources,
// providers, review queue, deployment, troubleshooting).
const sidebars: SidebarsConfig = {
  tutorialSidebar: ["intro"],
};

export default sidebars;
