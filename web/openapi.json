{
  "openapi": "3.1.0",
  "info": {
    "title": "tsundoku API",
    "description": "Manga discovery service. Read endpoints (`GET`) are public unless `auth.read_requires_auth = true`. Write endpoints (`POST`) always require the admin bearer.",
    "contact": {
      "name": "Sylvain Cau",
      "email": "sylvain.cau@gmail.com"
    },
    "license": {
      "name": "MIT OR Apache-2.0",
      "identifier": "MIT OR Apache-2.0"
    },
    "version": "1.8.1"
  },
  "paths": {
    "/api/v1/codex/refresh": {
      "post": {
        "tags": [
          "codex"
        ],
        "summary": "Trigger a Codex presence sweep. Shares the single codex lock with the cron,\nso a manual kick during a scheduled sweep is reported `skipped`.",
        "operationId": "codex_refresh",
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CodexRefreshResponse"
                }
              }
            }
          },
          "503": {
            "description": "Codex integration is disabled"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/codex/status": {
      "get": {
        "tags": [
          "codex"
        ],
        "summary": "Codex connection-health status for the admin UI.",
        "operationId": "codex_status",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CodexStatusDto"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/covers/by-url": {
      "get": {
        "tags": [
          "covers"
        ],
        "summary": "Proxy + cache an arbitrary cover URL after host-allowlist validation.",
        "operationId": "get_cover_by_url",
        "parameters": [
          {
            "name": "url",
            "in": "query",
            "description": "Absolute HTTPS URL of the cover image. Must be on the allowlist.",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Cover image bytes",
            "content": {
              "image/*": {}
            }
          },
          "400": {
            "description": "Missing, invalid, or disallowed URL"
          },
          "502": {
            "description": "Upstream fetch failed"
          },
          "503": {
            "description": "Cover cache directory not configured"
          }
        }
      }
    },
    "/api/v1/covers/invalidate-cache": {
      "post": {
        "tags": [
          "covers"
        ],
        "summary": "Wipe every file directly under `cover_cache_dir`. Subdirectories are\npreserved so an operator who keeps a sibling directory for unrelated\nartifacts does not lose it.",
        "operationId": "invalidate_cover_cache",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvalidateCoverCacheResponse"
                }
              }
            }
          },
          "503": {
            "description": "Cover cache directory not configured"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/covers/{series_id}": {
      "get": {
        "tags": [
          "covers"
        ],
        "summary": "Serve the cached cover for a series, fetching upstream once on miss.",
        "operationId": "get_cover_by_series_id",
        "parameters": [
          {
            "name": "series_id",
            "in": "path",
            "description": "Internal series id",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Cover image bytes",
            "content": {
              "image/*": {}
            }
          },
          "404": {
            "description": "Series missing or has no cover URL"
          },
          "502": {
            "description": "Upstream fetch failed"
          },
          "503": {
            "description": "Cover cache directory not configured"
          }
        }
      }
    },
    "/api/v1/events/jobs": {
      "get": {
        "tags": [
          "system"
        ],
        "summary": "Subscribe to manual-trigger lifecycle events as an SSE stream.",
        "description": "Auth: inherits the read auth gate (`auth.read_requires_auth`). The\nbrowser `EventSource` API cannot carry a custom `Authorization`\nheader, so installs that flip `read_requires_auth=true` will need\nthe operator's `api_key` to be sent via the\n`Authorization` header through a fetch-based polyfill, or the gate\ndisabled for this single route. Default installs (`false`) work out\nof the box.",
        "operationId": "events_jobs",
        "responses": {
          "200": {
            "description": "text/event-stream of JobEvent frames",
            "content": {
              "text/event-stream": {}
            }
          }
        }
      }
    },
    "/api/v1/genres": {
      "get": {
        "tags": [
          "tagging"
        ],
        "summary": "Every genre that's been observed at least once, sorted by descending\nseries count, then name. Powers the feed-filter autocomplete.",
        "operationId": "list_genres",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TagList"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/health": {
      "get": {
        "tags": [
          "system"
        ],
        "summary": "Pings the database and reports `ok` when reachable.",
        "operationId": "health",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Health"
                }
              }
            }
          },
          "503": {
            "description": "Database unreachable"
          }
        }
      }
    },
    "/api/v1/info": {
      "get": {
        "tags": [
          "system"
        ],
        "summary": "Returns the running binary's name and semver. Cheap, public, cache-forever.",
        "operationId": "info",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppInfo"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics/id-maps": {
      "get": {
        "tags": [
          "metrics"
        ],
        "summary": "Foreign-id map sizes: `series_external_ids` counts grouped by provider,\nplus persisted MangaUpdates legacy → modern slug cache stats. Used by\nthe admin \"ID Maps\" page; no range/bucketing applies because these\nnumbers are not time-series.",
        "operationId": "metrics_id_maps",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IdMapMetrics"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics/providers": {
      "get": {
        "tags": [
          "metrics"
        ],
        "summary": "Across-all-providers refresh summary.",
        "operationId": "metrics_providers_summary",
        "parameters": [
          {
            "name": "range",
            "in": "query",
            "description": "Sliding window for the aggregate, e.g. `24h`, `7d`. Defaults to 24h.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "buckets",
            "in": "query",
            "description": "Number of equal-width buckets. Clamped to [4, 168]. Defaults to 24.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProviderMetricsSummary"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics/providers/{id}": {
      "get": {
        "tags": [
          "metrics"
        ],
        "summary": "Detailed refresh metrics for a single provider.",
        "operationId": "providers_detail",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Provider id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "range",
            "in": "query",
            "description": "Sliding window for the aggregate, e.g. `24h`, `7d`. Defaults to 24h.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "buckets",
            "in": "query",
            "description": "Number of equal-width buckets. Clamped to [4, 168]. Defaults to 24.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProviderMetricsDetail"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics/review-queue": {
      "get": {
        "tags": [
          "metrics"
        ],
        "summary": "Review-queue depth-over-time + median time-to-decision for the window.",
        "operationId": "review_queue",
        "parameters": [
          {
            "name": "range",
            "in": "query",
            "description": "Sliding window for the aggregate, e.g. `24h`, `7d`. Defaults to 24h.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "buckets",
            "in": "query",
            "description": "Number of equal-width buckets. Clamped to [4, 168]. Defaults to 24.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReviewQueueMetrics"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics/sources": {
      "get": {
        "tags": [
          "metrics"
        ],
        "summary": "Across-all-sources summary over the requested window.",
        "operationId": "metrics_sources_summary",
        "parameters": [
          {
            "name": "range",
            "in": "query",
            "description": "Sliding window for the aggregate, e.g. `24h`, `7d`. Defaults to 24h.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "buckets",
            "in": "query",
            "description": "Number of equal-width buckets. Clamped to [4, 168]. Defaults to 24.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SourceMetricsSummary"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/metrics/sources/{name}": {
      "get": {
        "tags": [
          "metrics"
        ],
        "summary": "Detailed metrics for a single source: summary + per-bucket counts.",
        "operationId": "sources_detail",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Source instance name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "range",
            "in": "query",
            "description": "Sliding window for the aggregate, e.g. `24h`, `7d`. Defaults to 24h.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "buckets",
            "in": "query",
            "description": "Number of equal-width buckets. Clamped to [4, 168]. Defaults to 24.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SourceMetricsDetail"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/providers": {
      "get": {
        "tags": [
          "providers"
        ],
        "summary": "List every registered metadata provider with its latest cache-refresh\nmarkers and the active-provider flag.",
        "operationId": "list_providers",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProviderList"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/providers/refresh-all": {
      "post": {
        "tags": [
          "providers"
        ],
        "summary": "Fan-out cache refresh for every registered provider. Returns a per-id\ntriggered/skipped breakdown so the admin UI can render each result.",
        "operationId": "refresh_all_providers",
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RefreshAllResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/providers/{id}/refresh-cache": {
      "post": {
        "tags": [
          "providers"
        ],
        "summary": "Trigger an offline-cache refresh for the named provider. Same locking\nsemantics as the scheduled refresh job; an in-flight refresh causes\nthis request to report `skipped = true` without spawning a duplicate.",
        "operationId": "refresh_cache",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Provider id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RefreshResponse"
                }
              }
            }
          },
          "404": {
            "description": "No provider with that id registered"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/providers/{id}/search": {
      "get": {
        "tags": [
          "providers"
        ],
        "summary": "Title-or-externalId search against a single provider. Powers the\nreview-queue \"Link release\" modal.",
        "description": "- `?externalId=<id>` — direct lookup via `MetadataProvider::get`.\n- `?q=<title>` — `MetadataProvider::search`, then enrichment via\n  `get` on the top N hits and Dice-rescoring against `q`.\n- both empty → `400 Bad Request`.\n- unknown `id` → `404 Not Found`.",
        "operationId": "search_provider",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Provider id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "Free-text title query. Trimmed before use; ignored when empty.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "externalId",
            "in": "query",
            "description": "Direct provider external-id lookup. When set, the handler short-\ncircuits to a single hit with `score = 1.0`. May be a bare id or a\nfull provider URL (the host is auto-detected).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "foreignProvider",
            "in": "query",
            "description": "Canonical id of the provider the `externalId` belongs to, when it is\na *foreign* id resolved through the path provider's\n`resolve_by_foreign_id` (e.g. `foreignProvider=mangaupdates` against\nthe MangaBaka provider). Ignored when it equals the path provider.\nA full provider URL in `externalId` overrides this via host detection.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Maximum number of hits to enrich with full metadata. Defaults to\n10; clamped to `[1, 50]` to keep the per-request cost bounded.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProviderSearchResponse"
                }
              }
            }
          },
          "400": {
            "description": "Both q and externalId missing or empty"
          },
          "404": {
            "description": "Unknown provider id"
          }
        }
      }
    },
    "/api/v1/releases": {
      "get": {
        "tags": [
          "releases"
        ],
        "summary": "List releases ordered by `observed_at` descending. Filters compose.",
        "operationId": "list_releases",
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Filter by resolution status (`resolved`, `unresolved`, `ambiguous`,\n`review_pending`, `rejected`, `standalone`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sourceKind",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sourceName",
            "in": "query",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "seriesId",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReleasePage"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/releases/bulk/link": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Resolve a link request into the concrete `series.id` it targets, shared by\nthe single-release [`link`] handler and the [`bulk_link`] action. The\n`posted_at` anchor is only consulted on the provider path, where it seeds\nthe freshly-upserted series' first-seen timestamp; callers pass the\nrelease's own `posted_at` (single link) or the anchor release's (bulk).\nBulk-link a set of review-queue releases to one series. The body's `ids`\n(intersected with the queue statuses) select the releases; the series is\nnamed exactly as in the single-release [`link`] handler. Each release is\nlinked individually (clearing its candidates and bumping the series'\nhighest volume/chapter marks) so the result matches what N single links\nwould produce. The operator is responsible for the selection being one\nseries' releases; the endpoint links them blindly to the chosen target.",
        "operationId": "bulk_link",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkLinkRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkLinkResponse"
                }
              }
            }
          },
          "400": {
            "description": "Empty ids, malformed target, or unknown provider/external_id"
          },
          "404": {
            "description": "Target series not found"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/bulk/reject": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Bulk-reject a set of review-queue releases. The body's `ids` (or, when\nempty, the filter fields) select the target set; every matched release is\npinned to `rejected` and its candidates dropped in one set-based update.",
        "operationId": "bulk_reject",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkReviewRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkRejectResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/bulk/retry": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Bulk-retry a set of review-queue releases. The body's `ids` (or, when\nempty, the filter fields) select the target set (capped at the per-batch\nlimit). Spawns a background batch under the shared retry-all lock and\nreturns immediately; a concurrent retry-all / bulk-retry reports\n`skipped: true`. An empty match set is reported as `triggered: false`\nwith `matched: 0` (no batch spawned, nothing skipped).",
        "operationId": "bulk_retry",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BulkReviewRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkRetryResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/retry-all": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Re-run the resolver against every release currently visible in the\nreview queue (`unresolved`, `ambiguous`, `review_pending`). With\n`?includeResolved=true`, also walks `resolved` rows (skipping\nmanually-linked ones). Spawns a background task and returns\nimmediately so the request doesn't block on what can be a multi-\nminute walk. A dedicated per-process lock prevents a second click\nfrom spawning a parallel walk; in that case the response is\n`{ triggered: false, skipped: true }`.",
        "operationId": "retry_all",
        "parameters": [
          {
            "name": "includeResolved",
            "in": "query",
            "description": "Also re-evaluate rows currently marked `resolved` (excluding\nmanually-linked ones). Use after changing format-type rules,\ntitle-cleanup config, or the active provider's cache, when\npreviously-confident matches need to be reconsidered against the\nnew logic. `rejected`, `standalone`, and `manual`-path rows are\nalways excluded.",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RetryAllResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/unresolved": {
      "get": {
        "tags": [
          "releases"
        ],
        "summary": "Review queue: releases awaiting human attention.",
        "description": "Returns releases whose status is `unresolved`, `ambiguous`, or\n`review_pending`, each with the recorded review candidates so the UI\ncan render a \"pick the right match\" panel without a second fetch.",
        "operationId": "list_unresolved",
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "description": "1-indexed page number.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "description": "Items per page (capped server-side at 200).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "Free-text title substring match (case-insensitive). Whitespace-only\nis treated as absent.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sourceName",
            "in": "query",
            "description": "Restrict to a single source instance (`releases.source_name`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "format",
            "in": "query",
            "description": "Restrict to releases carrying this file format (e.g. `cbz`, `epub`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Narrow to one queue status. Ignored unless it's one of\n`unresolved` / `ambiguous` / `review_pending`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "searchQuery",
            "in": "query",
            "description": "Group filter: select only releases whose cleaned `search_queries`\ncontains this exact value (within the [`breadth`] index bound). Kept\ndistinct from the title-substring [`q`] filter — this is the handoff\nfrom a clicked release group, not free-text search. Whitespace-only is\ntreated as absent.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "breadth",
            "in": "query",
            "description": "How many leading `search_queries` variants the [`search_query`] /\ngrouping match considers: `1` = primary `[0]` only (default), `2` =\n`[0..2)`, `3` = all elements. Out-of-range/absent clamps to `1`.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "sort",
            "in": "query",
            "description": "Result ordering. One of `observed_desc` (default), `observed_asc`,\n`posted_desc`, `posted_asc`, `title_asc`, `title_desc`. Title sorts\nare case-insensitive. An unknown value falls back to `observed_desc`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnresolvedPage"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/releases/unresolved/groups": {
      "get": {
        "tags": [
          "releases"
        ],
        "summary": "Grouped review queue: clusters of releases sharing a cleaned search query.",
        "description": "Honors the same `q` / `sourceName` / `format` / `status` filters as the\nlist endpoint, plus `breadth` (1 = primary query only, default; 2 = first\ntwo; 3 = all). Only clusters with more than one member are returned,\nordered by descending count. Clicking a group re-filters the list via\n`searchQuery` (at the echoed `breadth`) to reproduce exactly these members.\nPagination/sort params are ignored.",
        "operationId": "list_groups",
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "description": "1-indexed page number.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "description": "Items per page (capped server-side at 200).",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "Free-text title substring match (case-insensitive). Whitespace-only\nis treated as absent.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sourceName",
            "in": "query",
            "description": "Restrict to a single source instance (`releases.source_name`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "format",
            "in": "query",
            "description": "Restrict to releases carrying this file format (e.g. `cbz`, `epub`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Narrow to one queue status. Ignored unless it's one of\n`unresolved` / `ambiguous` / `review_pending`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "searchQuery",
            "in": "query",
            "description": "Group filter: select only releases whose cleaned `search_queries`\ncontains this exact value (within the [`breadth`] index bound). Kept\ndistinct from the title-substring [`q`] filter — this is the handoff\nfrom a clicked release group, not free-text search. Whitespace-only is\ntreated as absent.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "breadth",
            "in": "query",
            "description": "How many leading `search_queries` variants the [`search_query`] /\ngrouping match considers: `1` = primary `[0]` only (default), `2` =\n`[0..2)`, `3` = all elements. Out-of-range/absent clamps to `1`.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "sort",
            "in": "query",
            "description": "Result ordering. One of `observed_desc` (default), `observed_asc`,\n`posted_desc`, `posted_asc`, `title_asc`, `title_desc`. Title sorts\nare case-insensitive. An unknown value falls back to `observed_desc`.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReleaseGroupsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/releases/{id}/keep": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Mark a release as a worthwhile standalone item that is not (and will\nnever be) a tracked series: a guidebook, an artbook, a one-shot. Drops\ncandidates and pins the status to `standalone` so the resolver leaves it\nalone; unlike `rejected`, these stay browsable in the \"Kept\" view.\nRe-run `retry` to pull one back into the resolution pipeline.",
        "operationId": "keep",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Release id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReleaseDto"
                }
              }
            }
          },
          "404": {
            "description": "Release not found"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/{id}/link": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Manually link a release to a series. Body shape:",
        "description": "- `{ \"seriesId\": 42 }` — link to an existing series row by internal id.\n- `{ \"provider\": \"mangabaka\", \"externalId\": \"1677\" }` — link by a\n  provider's external id. If no `series_external_ids` row matches yet,\n  the active provider's `get` is called to fetch metadata and create\n  the series row before linking.",
        "operationId": "link",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Release id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/LinkRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReleaseDto"
                }
              }
            }
          },
          "400": {
            "description": "Provider not registered or external_id unknown to provider"
          },
          "404": {
            "description": "Release or series not found"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/{id}/reject": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Mark a release as \"not a series we care about\". Drops candidates and\npins the resolution status to `rejected` so the resolver leaves it\nalone on subsequent runs.",
        "operationId": "reject",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Release id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReleaseDto"
                }
              }
            }
          },
          "404": {
            "description": "Release not found"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/releases/{id}/retry": {
      "post": {
        "tags": [
          "releases"
        ],
        "summary": "Re-run the resolver against a single release. Useful after a provider\nrefresh, a config change, or a manual edit.",
        "operationId": "retry",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Release id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReleaseDto"
                }
              }
            }
          },
          "404": {
            "description": "Release not found"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/series": {
      "get": {
        "tags": [
          "series"
        ],
        "summary": "List series ordered by last release timestamp (most recent first by default).",
        "operationId": "list_series",
        "parameters": [
          {
            "name": "page",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "pageSize",
            "in": "query",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          },
          {
            "name": "kind",
            "in": "query",
            "description": "Filter by stored `series.type` (e.g. `manga`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "status",
            "in": "query",
            "description": "Filter by stored `series.status` (e.g. `ongoing`).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "owned",
            "in": "query",
            "description": "Filter by ownership flag (true = owned by Codex, false = discoverable).",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "hasReleases",
            "in": "query",
            "description": "Filter by whether any releases are linked to the series. `true`\nkeeps only series with ≥1 release; `false` keeps only orphaned\nseries (zero releases — often the residue of a manual re-link).",
            "required": false,
            "schema": {
              "type": "boolean"
            }
          },
          {
            "name": "genres",
            "in": "query",
            "description": "Comma-separated genre names. Combined with [`Self::genres_mode`]:\n`any` (default) keeps series matching at least one; `all` keeps\nonly series matching every entry. Each entry is matched case-\ninsensitively. AND-combined with the other filters.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "genresMode",
            "in": "query",
            "description": "`any` (default) or `all`. See [`Self::genres`].",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "tags",
            "in": "query",
            "description": "Comma-separated tag names. Mirrors [`Self::genres`].",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "tagsMode",
            "in": "query",
            "description": "`any` (default) or `all`. See [`Self::tags`].",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sort",
            "in": "query",
            "description": "Sort field. Supports `last_release_at` (default), `first_seen_at`,\n`total_volumes`, `total_chapters`, `highest_volume`, and\n`highest_chapter`. The count / highest sorts are nullable-aware:\nrows without a value sink to the end regardless of direction.\nIgnored when `q` is present (results are ranked by relevance instead).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "order",
            "in": "query",
            "description": "`asc` or `desc` (default).",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "in": "query",
            "description": "Free-text query. When set, results are ranked by a Dice-coefficient\nscore against canonical + alternate titles, with a boost for FTS5\nprefix matches. Whitespace-only is treated as absent.",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "codexStatus",
            "in": "query",
            "description": "Filter by Codex presence status: `any` (on Codex), `missing` (not on\nCodex), `complete`, `behind`, or `present`. **Admin-only and enforced\nserver-side**: for a non-admin request the param is ignored entirely,\nso it can't be used to probe library contents.",
            "required": false,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeriesListPage"
                }
              }
            }
          }
        }
      },
      "post": {
        "tags": [
          "series"
        ],
        "summary": "Create a manual series: a provider-less catalog entry for a real series\nthe active provider lacks. The operator then links releases to it via\n`POST /releases/{id}/link` with `{ \"seriesId\": N }`.",
        "description": "No `series_external_ids` row is created, so this series never\nparticipates in auto-resolution (the fuzzy resolver searches the\nprovider, not the local catalog) and is skipped by metadata refresh.",
        "operationId": "create",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateSeriesRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeriesDetail"
                }
              }
            }
          },
          "400": {
            "description": "canonicalTitle is empty"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/series/invalidate-metadata-hashes": {
      "post": {
        "tags": [
          "series"
        ],
        "summary": "Clear `metadata_hash` for every provider-backed series row in scope so\nthe next refresh tick rewrites them. The persist layer short-circuits\nthe series UPDATE when the incoming provider payload hashes to the\nstored value; that's the right call for steady-state refreshes, but it\nstrands existing rows whenever a new denormalized column lands on the\n`series` table (the upstream payload is unchanged → hash matches →\nwrite skipped → new column stays NULL forever).",
        "description": "This endpoint is the operator escape hatch for that scenario. It runs\nsynchronously and returns the affected count; the operator then\ntriggers `/series/refresh-all` (or waits for the next cron tick) to\nactually rewrite the rows.\n\nManual rows (`metadata_source = 'manual'`) are always left alone.",
        "operationId": "invalidate_series_metadata_hashes",
        "parameters": [
          {
            "name": "provider",
            "in": "query",
            "description": "Provider id to scope the invalidation to (e.g. `\"mangabaka\"`).\nOptional. When absent, every provider-backed row is affected.",
            "required": false,
            "schema": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InvalidateMetadataHashesResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/series/recompute-spans": {
      "post": {
        "tags": [
          "series"
        ],
        "summary": "Recompute every release's volume/chapter span and every series'\n`highest_volume` / `highest_chapter` mark from the stored file lists\n(titles as fallback). Network-free and idempotent. Unlike the\nincremental bump that runs when a release is linked, this is an\nauthoritative pass: a series' marks are *replaced* with the MAX across\nits currently-linked releases, so it also corrects values an earlier,\nmore eager parser over-counted and clears marks on series whose releases\nno longer parse to anything.",
        "description": "Use it after changing the span-parsing logic, or to backfill a catalog\nwhose releases predate span detection. Runs in-process against the same\npool `serve` uses, so it is safe to trigger on a live instance (it shares\nthe single SQLite writer rather than contending with a separate process).",
        "operationId": "recompute_series_spans",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecomputeSpansResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/series/refresh-all": {
      "post": {
        "tags": [
          "series"
        ],
        "summary": "Trigger a bulk refresh of stale series rows against the active\nmetadata provider. The spawned tick reads `batch_size` and\n`min_age_days` from `metadata.series_refresh`; the same selection\nquery backs the cron, so a manual click and a cron tick are\nbehaviourally identical (and they share a per-provider mutex, so\nthey can't race).",
        "description": "Returns immediately with `triggered: true` once the tick is spawned,\nor `triggered: false, skipped: true` when a refresh is already in\nflight for the active provider.",
        "operationId": "refresh_all_series",
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RefreshAllSeriesResponse"
                }
              }
            }
          },
          "503": {
            "description": "Active provider is not registered"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/series/{id}": {
      "get": {
        "tags": [
          "series"
        ],
        "summary": "Series detail, including the resolved external-ID mappings.",
        "operationId": "get",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Internal series id",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeriesDetail"
                }
              }
            }
          },
          "404": {
            "description": "No series with that id"
          }
        }
      }
    },
    "/api/v1/series/{id}/codex-link": {
      "post": {
        "tags": [
          "codex"
        ],
        "summary": "Hand-link a tsundoku series to a Codex series UUID. For series with no\nmatchable external id; the next sweep refreshes the link's counts by uuid.",
        "operationId": "codex_link",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "tsundoku series id",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CodexLinkRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CodexLinkResponse"
                }
              }
            }
          },
          "404": {
            "description": "No series with that id"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      },
      "delete": {
        "tags": [
          "codex"
        ],
        "summary": "Remove a series' Codex link (manual or auto). Idempotent: unlinking a\nseries with no link is a no-op success.",
        "operationId": "codex_unlink",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "tsundoku series id",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Link removed (or none existed)"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/series/{id}/refresh-metadata": {
      "post": {
        "tags": [
          "series"
        ],
        "summary": "Re-fetch metadata for a series from the active provider and re-persist.",
        "operationId": "refresh_metadata",
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "description": "Internal series id",
            "required": true,
            "schema": {
              "type": "integer",
              "format": "int32"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SeriesDetail"
                }
              }
            }
          },
          "404": {
            "description": "Series or provider entry not found"
          },
          "409": {
            "description": "No mapping for the active provider on this series"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/sources": {
      "get": {
        "tags": [
          "sources"
        ],
        "summary": "List every registered discovery source with its last-poll markers.",
        "operationId": "list_sources",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SourceList"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/sources/poll-all": {
      "post": {
        "tags": [
          "sources"
        ],
        "summary": "Fan-out trigger for every registered source. Returns a per-source\ntriggered/skipped result without aggregating: callers see the same\nper-source outcomes a series of single-source calls would have produced.",
        "operationId": "poll_all",
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PollAllResponse"
                }
              }
            }
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/sources/{name}/backfill": {
      "post": {
        "tags": [
          "sources"
        ],
        "summary": "Trigger an in-process historical backfill for the named source. Runs\nthe same loop as the `tsundoku backfill` CLI, but inside the serve\nprocess under the shared per-source mutex, so it cannot race a cron\npoll (returns `skipped = true` when work is already in flight). Returns\n`422` when the source's kind does not support backfill.",
        "operationId": "backfill",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Source instance name",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "pages",
            "in": "query",
            "description": "Number of listing pages to walk, starting at page 1. Clamped to a\nminimum of 1.",
            "required": false,
            "schema": {
              "type": "integer",
              "format": "int32",
              "minimum": 0
            }
          }
        ],
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualBackfillResponse"
                }
              }
            }
          },
          "404": {
            "description": "No source with that name registered"
          },
          "422": {
            "description": "Source kind does not support historical backfill"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/sources/{name}/poll": {
      "post": {
        "tags": [
          "sources"
        ],
        "summary": "Trigger a one-shot poll for the named source. Uses the same per-source\nmutex the cron job holds, so a manual kick during a scheduled tick is\nsilently skipped (`skipped = true`).",
        "operationId": "poll",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Source instance name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualPollResponse"
                }
              }
            }
          },
          "404": {
            "description": "No source with that name registered"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/sources/{name}/re-enrich": {
      "post": {
        "tags": [
          "sources"
        ],
        "summary": "Re-fetch the detail page for the source's already-persisted releases that\nmatch `statuses`, refreshing their source-derived columns (files,\ndescription, extracted links, information URL) without touching resolution\nstate. Use after a parser change adds or fixes a detail-page field.\nShares the per-source mutex, so it cannot race a cron poll / backfill\n(returns `skipped = true` when work is already in flight).",
        "operationId": "reenrich",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "description": "Source instance name",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReenrichRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "202": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualReenrichResponse"
                }
              }
            }
          },
          "400": {
            "description": "Empty or unknown status in the request"
          },
          "404": {
            "description": "No source with that name registered"
          }
        },
        "security": [
          {
            "admin": []
          }
        ]
      }
    },
    "/api/v1/stats": {
      "get": {
        "tags": [
          "system"
        ],
        "summary": "Aggregate counts surfaced by the frontend home page and review badge.",
        "operationId": "stats",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StatsResponse"
                }
              }
            }
          }
        }
      }
    },
    "/api/v1/tags": {
      "get": {
        "tags": [
          "tagging"
        ],
        "summary": "Tag analog of [`list_genres`]. Same shape, different table.",
        "operationId": "list_tags",
        "responses": {
          "200": {
            "description": "",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TagList"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ApiErrorBody": {
        "type": "object",
        "required": [
          "error",
          "message"
        ],
        "properties": {
          "error": {
            "type": "string"
          },
          "message": {
            "type": "string"
          }
        }
      },
      "AppInfo": {
        "type": "object",
        "required": [
          "name",
          "version"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "version": {
            "type": "string"
          }
        }
      },
      "BulkLinkRequest": {
        "type": "object",
        "description": "Body for [`bulk_link`]: link every release in `ids` to a single series.\nThe target series is named exactly as in [`LinkRequest`] — either an\nexisting `seriesId`, or a `provider` + `externalId` pair the active\nprovider can resolve (materializing the series row on first link). The\n`ids` are intersected with the queue statuses server-side so a decided\nrelease can't be relinked, and an empty `ids` list is rejected: linking a\nwhole filtered set to one series is never what the operator means.",
        "properties": {
          "externalId": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": []
          },
          "provider": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "seriesId": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "default": null
          }
        }
      },
      "BulkLinkResponse": {
        "type": "object",
        "required": [
          "linked",
          "seriesId"
        ],
        "properties": {
          "linked": {
            "type": "integer",
            "format": "int64",
            "description": "Number of releases linked to the target series.",
            "minimum": 0
          },
          "seriesId": {
            "type": "integer",
            "format": "int32",
            "description": "The series the releases were linked to."
          }
        }
      },
      "BulkRejectResponse": {
        "type": "object",
        "required": [
          "rejected"
        ],
        "properties": {
          "rejected": {
            "type": "integer",
            "format": "int64",
            "description": "Number of releases moved to `rejected`.",
            "minimum": 0
          }
        }
      },
      "BulkRetryResponse": {
        "type": "object",
        "required": [
          "triggered",
          "skipped",
          "matched"
        ],
        "properties": {
          "matched": {
            "type": "integer",
            "format": "int64",
            "description": "Number of releases the filters/ids matched (the batch size, capped at\nthe per-batch limit).",
            "minimum": 0
          },
          "skipped": {
            "type": "boolean",
            "description": "`true` when a prior retry batch is still in flight; the request is a no-op."
          },
          "triggered": {
            "type": "boolean",
            "description": "`true` when a batch was spawned by this request."
          }
        }
      },
      "BulkReviewRequest": {
        "type": "object",
        "description": "Shared body for the bulk review actions. The target set is either an\nexplicit `ids` list (when non-empty) or every queue release matching the\nfilter fields. An all-empty body targets the entire queue. The filters\nmirror [`ReviewQueueQuery`] so \"select all matching\" acts on exactly what\nthe list endpoint shows; explicit `ids` are still intersected with the\nqueue statuses so a decided release can't be re-acted on.",
        "properties": {
          "breadth": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Grouping breadth mirroring [`ReviewQueueQuery::breadth`] (clamped to\n{1,2,3}, default 1).",
            "default": null,
            "minimum": 0
          },
          "format": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "ids": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "default": []
          },
          "q": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "searchQuery": {
            "type": [
              "string",
              "null"
            ],
            "description": "Release-group filter mirroring [`ReviewQueueQuery::search_query`], so\n\"reject/retry the whole group\" acts on exactly the releases the chip\nshows.",
            "default": null
          },
          "sourceName": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          },
          "status": {
            "type": [
              "string",
              "null"
            ],
            "default": null
          }
        }
      },
      "CodexInfo": {
        "type": "object",
        "description": "Admin-only presence object embedded on a series. Absent (the `codex` field\nis `None`) when the series is not on Codex or the caller is not an admin.",
        "required": [
          "status",
          "seriesUuid",
          "deepLink",
          "linkKind",
          "syncedAt"
        ],
        "properties": {
          "deepLink": {
            "type": "string",
            "description": "Deep link to the series page in Codex's web UI."
          },
          "linkKind": {
            "$ref": "#/components/schemas/CodexLinkKind"
          },
          "localMaxChapter": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Highest owned chapter on Codex (comparison basis)."
          },
          "localMaxVolume": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Highest owned volume on Codex (comparison basis)."
          },
          "seriesUuid": {
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/CodexStatus"
          },
          "syncedAt": {
            "type": "integer",
            "format": "int64",
            "description": "When this link was last written by a sweep."
          },
          "volumesOwned": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Approximate count of owned volumes (display-only; never compared)."
          }
        }
      },
      "CodexLinkKind": {
        "type": "string",
        "description": "How a link was established.",
        "enum": [
          "auto",
          "manual"
        ]
      },
      "CodexLinkRequest": {
        "type": "object",
        "required": [
          "codexSeriesUuid"
        ],
        "properties": {
          "codexSeriesUuid": {
            "type": "string",
            "description": "Codex series UUID to hand-link this tsundoku series to."
          }
        }
      },
      "CodexLinkResponse": {
        "type": "object",
        "required": [
          "seriesId",
          "codexSeriesUuid",
          "linkKind"
        ],
        "properties": {
          "codexSeriesUuid": {
            "type": "string"
          },
          "linkKind": {
            "type": "string",
            "description": "Always `manual` for a hand-link."
          },
          "seriesId": {
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "CodexRefreshResponse": {
        "type": "object",
        "required": [
          "triggered",
          "skipped"
        ],
        "properties": {
          "skipped": {
            "type": "boolean",
            "description": "`false` when a previous sweep is still in flight; the request is a no-op."
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "CodexStatus": {
        "type": "string",
        "description": "Presence status for a series the operator owns on Codex.",
        "enum": [
          "complete",
          "behind",
          "present"
        ]
      },
      "CodexStatusDto": {
        "type": "object",
        "description": "Connection-health snapshot for the admin UI. When the integration is\ndisabled, only `enabled: false` is meaningful; the rest are `None`.",
        "required": [
          "enabled",
          "reachable",
          "authState"
        ],
        "properties": {
          "authState": {
            "type": "string",
            "description": "`unknown` | `ok` | `unauthorized` (401) | `forbidden` (403)."
          },
          "codexName": {
            "type": [
              "string",
              "null"
            ]
          },
          "codexVersion": {
            "type": [
              "string",
              "null"
            ]
          },
          "enabled": {
            "type": "boolean"
          },
          "fetchedCount": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Series pulled from Codex by the last successful sweep (superset of\n`linked_count`)."
          },
          "lastError": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastPreflightAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "lastSuccessAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "linkedCount": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Series matched to a tsundoku series by the last successful sweep."
          },
          "reachable": {
            "type": "boolean"
          }
        }
      },
      "CreateSeriesRequest": {
        "type": "object",
        "description": "Body for creating a manual series. Only `canonicalTitle` is required;\nthe rest are optional descriptive fields. No provider mapping is created\n(that's the whole point), so the series is provider-agnostic and\n`metadataSource` is pinned to `manual`.",
        "required": [
          "canonicalTitle"
        ],
        "properties": {
          "canonicalTitle": {
            "type": "string"
          },
          "coverUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "kind": {
            "type": [
              "string",
              "null"
            ]
          },
          "year": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          }
        }
      },
      "ErrorKindBucket": {
        "type": "object",
        "required": [
          "kind",
          "count"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "format": "int64"
          },
          "kind": {
            "type": "string",
            "description": "`None` is stored when a failure row pre-dates the error_kind helper\nor comes from an unwrapped legacy path; surface it as `unknown`."
          }
        }
      },
      "ExternalIdDto": {
        "type": "object",
        "required": [
          "provider",
          "externalId",
          "fetchedAt"
        ],
        "properties": {
          "externalId": {
            "type": "string"
          },
          "fetchedAt": {
            "type": "integer",
            "format": "int64"
          },
          "provider": {
            "type": "string"
          }
        }
      },
      "ExternalIdMapCount": {
        "type": "object",
        "required": [
          "provider",
          "count"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "format": "int64",
            "description": "Number of `(provider, external_id) → series_id` rows recorded."
          },
          "provider": {
            "type": "string",
            "description": "Canonical provider id (e.g. `mangaupdates`, `mal`, `anilist`)."
          }
        }
      },
      "ExtractedLinksDto": {
        "type": "object",
        "description": "External provider links scraped from a release's description. Mirrors\n`td_source::ExternalLinks`; redefined here so we don't pull utoipa into\nthe domain crate.",
        "properties": {
          "anilist": {
            "type": [
              "string",
              "null"
            ]
          },
          "mal": {
            "type": [
              "string",
              "null"
            ]
          },
          "mangabaka": {
            "type": [
              "string",
              "null"
            ]
          },
          "mangadex": {
            "type": [
              "string",
              "null"
            ]
          },
          "mangaupdates": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "FetchLatencyDto": {
        "type": "object",
        "properties": {
          "maxMs": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "p50Ms": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "p95Ms": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          }
        }
      },
      "Health": {
        "type": "object",
        "required": [
          "status"
        ],
        "properties": {
          "status": {
            "type": "string"
          }
        }
      },
      "IdMapMetrics": {
        "type": "object",
        "required": [
          "externalIds",
          "mangaupdatesRedirectCache"
        ],
        "properties": {
          "externalIds": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExternalIdMapCount"
            },
            "description": "Per-provider row counts from `series_external_ids`. Sorted\nalphabetically by provider for stable rendering."
          },
          "mangaupdatesRedirectCache": {
            "$ref": "#/components/schemas/MangaupdatesRedirectStats",
            "description": "State of the persisted MangaUpdates legacy → modern slug cache."
          }
        }
      },
      "InFlight": {
        "type": "object",
        "description": "Currently-running marker hung off each source / provider listing entry\nso the admin UI can render the \"RUNNING…\" pill straight from the DTO\non a fresh page load, without waiting for the SSE channel to replay\nstate it never had.\n\nHydrated from the matching `*_runs` row whose `status = 'running'`.\n`progress` is populated when the in-flight row's `progress_current` /\n`progress_total` / `progress_phase` columns are non-`NULL` (i.e. the\njob is reporting progress); jobs that don't report leave it `None`\nand the pill shows the binary \"is in flight\" state.",
        "required": [
          "startedAt"
        ],
        "properties": {
          "progress": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/JobProgress"
              }
            ]
          },
          "startedAt": {
            "type": "integer",
            "format": "int64",
            "description": "Epoch seconds the in-flight run started at."
          }
        }
      },
      "InvalidateCoverCacheResponse": {
        "type": "object",
        "description": "Response payload for `POST /api/v1/covers/invalidate-cache`.",
        "required": [
          "filesDeleted",
          "bytesFreed"
        ],
        "properties": {
          "bytesFreed": {
            "type": "integer",
            "format": "int64",
            "description": "Total bytes freed across the deleted files.",
            "minimum": 0
          },
          "filesDeleted": {
            "type": "integer",
            "format": "int32",
            "description": "Number of files deleted from `cover_cache_dir`. Subdirectories\nand hidden files are ignored.",
            "minimum": 0
          }
        }
      },
      "InvalidateMetadataHashesResponse": {
        "type": "object",
        "description": "Response from `POST /api/v1/series/invalidate-metadata-hashes`. The\nendpoint runs synchronously, so the counts are the actual outcome of\nthe call (not \"queued work\").",
        "required": [
          "invalidated",
          "skippedManual"
        ],
        "properties": {
          "invalidated": {
            "type": "integer",
            "format": "int32",
            "description": "Number of `series` rows whose `metadata_hash` was cleared. A\nsubsequent series-refresh tick will rewrite each one because the\nhash short-circuit no longer fires.",
            "minimum": 0
          },
          "provider": {
            "type": [
              "string",
              "null"
            ],
            "description": "Echoes the `provider` query parameter, or `null` when the call\nwas not scoped."
          },
          "skippedManual": {
            "type": "integer",
            "format": "int32",
            "description": "Number of rows that matched the scope but were left untouched\nbecause `metadata_source = 'manual'`. Reported so the operator\ncan spot when a meaningful chunk of the catalog is curated by\nhand (and therefore not eligible for upstream refresh).",
            "minimum": 0
          }
        }
      },
      "JobEvent": {
        "type": "object",
        "description": "Single broadcast frame for the SSE channel. Always has `kind`, `id`,\n`phase`, and `at` (epoch millis); `result` is only populated on\n`Finished` events; `progress` is only populated on `Progress` events.",
        "required": [
          "kind",
          "id",
          "phase",
          "at"
        ],
        "properties": {
          "at": {
            "type": "integer",
            "format": "int64"
          },
          "id": {
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/JobKind"
          },
          "phase": {
            "$ref": "#/components/schemas/JobPhase"
          },
          "progress": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/JobProgress"
              }
            ]
          },
          "result": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/JobResult"
              }
            ]
          }
        }
      },
      "JobKind": {
        "type": "string",
        "description": "Kind of work an event refers to. Stays a plain string in the\nserialized form so the frontend can pattern-match without importing\nthe enum.",
        "enum": [
          "source",
          "provider",
          "seriesRefresh",
          "codex"
        ]
      },
      "JobPhase": {
        "type": "string",
        "description": "Lifecycle phase. `Started` fires after the per-key mutex was\nacquired (so a `skipped` job emits only `Finished`). `Progress`\nframes fire from inside the work body via\n[`ProgressHandle`](crate::jobs::progress::ProgressHandle); they are\nnot throttled the way DB progress writes are — the broadcast channel\nback-pressure handles laggy consumers by dropping intermediate frames.",
        "enum": [
          "started",
          "progress",
          "finished"
        ]
      },
      "JobProgress": {
        "type": "object",
        "description": "Progress payload attached to a `Progress` SSE frame and to the\n`InFlight` DTO when the in-flight row carries a last-checkpoint\nsnapshot.\n\n`total` is optional because some phases of some jobs don't have a\nmeaningful upper bound (e.g. the tar-extract phase of the provider\ncache refresh, which streams files of unknown count); a `current`-only\npayload still lets the UI render \"doing N units\" with no fraction.",
        "required": [
          "current"
        ],
        "properties": {
          "current": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "phase": {
            "type": [
              "string",
              "null"
            ],
            "description": "Free-text label for multi-stage jobs. Provider cache refresh uses\nit for `\"downloading\"` / `\"extracting\"` / `\"indexing\"`; other jobs\nleave it `None`."
          },
          "total": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "JobResult": {
        "type": "object",
        "description": "Compact result payload attached to a `finished` event. Optional\nper-trigger fields (fetched / new / resolved / bytes) are `None`\nwhen the trigger doesn't produce them.",
        "required": [
          "triggered",
          "skipped"
        ],
        "properties": {
          "bytes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "fetched": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "new": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "resolved": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "skipped": {
            "type": "boolean"
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "LinkRequest": {
        "type": "object",
        "description": "Body for the manual-link endpoint. Exactly one of:\n- `seriesId`: link to an existing series row by internal id.\n- `provider` + `externalId`: link via the named provider's external id;\n  the provider's `get` is called when no mapping exists yet.",
        "properties": {
          "externalId": {
            "type": [
              "string",
              "null"
            ]
          },
          "provider": {
            "type": [
              "string",
              "null"
            ]
          },
          "seriesId": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          }
        }
      },
      "MangaupdatesRedirectStats": {
        "type": "object",
        "required": [
          "modernCount",
          "tombstoneCount"
        ],
        "properties": {
          "lastResolvedAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Epoch seconds of the most recent translation. `None` when the\ncache is empty."
          },
          "modernCount": {
            "type": "integer",
            "format": "int64",
            "description": "Rows where `modern_id IS NOT NULL` — legacy ids we've successfully\ntranslated to a slug."
          },
          "tombstoneCount": {
            "type": "integer",
            "format": "int64",
            "description": "Rows where `modern_id IS NULL` — legacy ids MU retired, so we no\nlonger waste a HEAD request on them."
          }
        }
      },
      "ManualBackfillResponse": {
        "type": "object",
        "required": [
          "source",
          "pages",
          "triggered",
          "skipped"
        ],
        "properties": {
          "pages": {
            "type": "integer",
            "format": "int32",
            "description": "Pages the run was asked to walk (after clamping); it may stop early\nif the source runs out of history.",
            "minimum": 0
          },
          "skipped": {
            "type": "boolean",
            "description": "`true` when a poll or backfill for this source was already in\nflight; the request is a no-op."
          },
          "source": {
            "type": "string"
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "ManualPollResponse": {
        "type": "object",
        "required": [
          "source",
          "triggered",
          "skipped"
        ],
        "properties": {
          "skipped": {
            "type": "boolean",
            "description": "`false` when a previous tick is still in flight; the request is a no-op."
          },
          "source": {
            "type": "string"
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "ManualReenrichResponse": {
        "type": "object",
        "required": [
          "source",
          "statuses",
          "triggered",
          "skipped"
        ],
        "properties": {
          "skipped": {
            "type": "boolean",
            "description": "`true` when a poll / backfill / re-enrich for this source was already\nin flight; the request is a no-op."
          },
          "source": {
            "type": "string"
          },
          "statuses": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "The (validated) statuses the run targets."
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "PollAllResponse": {
        "type": "object",
        "required": [
          "results"
        ],
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ManualPollResponse"
            }
          }
        }
      },
      "ProviderCacheState": {
        "type": "object",
        "required": [
          "fetchedAt"
        ],
        "properties": {
          "bytesDownloaded": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "cacheVersion": {
            "type": [
              "string",
              "null"
            ]
          },
          "fetchedAt": {
            "type": "integer",
            "format": "int64"
          },
          "recordCount": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "sourceUrl": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "ProviderConfigDto": {
        "type": "object",
        "description": "Per-provider config exposed to the admin UI. The api_key is reported as\na boolean only; the raw value never leaves the process.",
        "required": [
          "apiFallback",
          "apiKeySet",
          "apiBaseUrl",
          "offlineDumpConfigured",
          "offlineCacheLoaded",
          "negativeCacheTtlDays",
          "timeoutSeconds"
        ],
        "properties": {
          "apiBaseUrl": {
            "type": "string"
          },
          "apiFallback": {
            "type": "boolean"
          },
          "apiKeySet": {
            "type": "boolean",
            "description": "`true` when an api_key is configured (any non-empty value). The\nactual key never appears in JSON; that's the whole point of stashing\nit in `.env`."
          },
          "negativeCacheTtlDays": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "offlineCacheLoaded": {
            "type": "boolean",
            "description": "Runtime: whether the on-disk dump is currently loaded."
          },
          "offlineDumpConfigured": {
            "type": "boolean"
          },
          "offlineDumpUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "offlineRefreshCron": {
            "type": [
              "string",
              "null"
            ]
          },
          "timeoutSeconds": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "ProviderDto": {
        "type": "object",
        "required": [
          "id",
          "displayName",
          "active",
          "foreignSources"
        ],
        "properties": {
          "active": {
            "type": "boolean"
          },
          "config": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProviderConfigDto",
                "description": "Operator-facing snapshot of the `[providers.<id>]` config block. May\nbe `None` for providers that don't have a typed config block (today\nthis only happens for test doubles)."
              }
            ]
          },
          "displayName": {
            "type": "string"
          },
          "foreignSources": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Canonical ids of other providers this provider can cross-resolve via\n`resolve_by_foreign_id`. Drives the review modal's \"ID source\"\ndropdown. Empty for providers without foreign-ID resolution."
          },
          "id": {
            "type": "string"
          },
          "inFlight": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/InFlight",
                "description": "Set when a `provider_refreshes` row for this provider is currently\n`status = 'running'`. Lets the admin UI render the \"RUNNING…\" pill\nfrom a fresh page load instead of waiting for an SSE event the\nchannel doesn't replay."
              }
            ]
          },
          "lastRefresh": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProviderCacheState"
              }
            ]
          }
        }
      },
      "ProviderList": {
        "type": "object",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderDto"
            }
          }
        }
      },
      "ProviderMetricsBucket": {
        "type": "object",
        "required": [
          "bucketStart",
          "successCount",
          "failureCount",
          "skippedCount"
        ],
        "properties": {
          "bucketStart": {
            "type": "integer",
            "format": "int64"
          },
          "failureCount": {
            "type": "integer",
            "format": "int64"
          },
          "skippedCount": {
            "type": "integer",
            "format": "int64"
          },
          "successCount": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "ProviderMetricsDetail": {
        "type": "object",
        "required": [
          "providerId",
          "buckets",
          "fetchLatency",
          "bucketSeconds",
          "rangeSeconds",
          "since",
          "until"
        ],
        "properties": {
          "bucketSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "buckets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderMetricsBucket"
            }
          },
          "fetchLatency": {
            "$ref": "#/components/schemas/FetchLatencyDto"
          },
          "providerId": {
            "type": "string"
          },
          "rangeSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "since": {
            "type": "integer",
            "format": "int64"
          },
          "summary": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ProviderMetricsSummaryItem"
              }
            ]
          },
          "until": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "ProviderMetricsSummary": {
        "type": "object",
        "required": [
          "items",
          "rangeSeconds",
          "since",
          "until"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderMetricsSummaryItem"
            }
          },
          "rangeSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "since": {
            "type": "integer",
            "format": "int64"
          },
          "until": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "ProviderMetricsSummaryItem": {
        "type": "object",
        "required": [
          "providerId",
          "totalRuns",
          "successCount",
          "failureCount",
          "skippedCount"
        ],
        "properties": {
          "bytesSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "failureCount": {
            "type": "integer",
            "format": "int64"
          },
          "lastStartedAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "lastStatus": {
            "type": [
              "string",
              "null"
            ]
          },
          "providerId": {
            "type": "string"
          },
          "skippedCount": {
            "type": "integer",
            "format": "int64"
          },
          "successCount": {
            "type": "integer",
            "format": "int64"
          },
          "successRate": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "totalRuns": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "ProviderSearchHit": {
        "type": "object",
        "description": "One enriched hit. `score` is Dice(`q`, `title`) for the title-search\npath, or `1.0` for the externalId short-circuit path.",
        "required": [
          "externalId",
          "title",
          "genres",
          "tags",
          "score"
        ],
        "properties": {
          "coverUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "externalId": {
            "type": "string"
          },
          "genres": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "kind": {
            "type": [
              "string",
              "null"
            ]
          },
          "nativeTitle": {
            "type": [
              "string",
              "null"
            ],
            "description": "First alternate title, if any. Useful for showing the\nromaji/Japanese form alongside the canonical English title."
          },
          "score": {
            "type": "number",
            "format": "float"
          },
          "status": {
            "type": [
              "string",
              "null"
            ]
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "title": {
            "type": "string"
          },
          "totalChapters": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          },
          "totalVolumes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Published volume/chapter counts from provider metadata, shown next to\neach hit so the operator can match a release against the series length."
          },
          "year": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          }
        }
      },
      "ProviderSearchResponse": {
        "type": "object",
        "required": [
          "provider",
          "hits"
        ],
        "properties": {
          "hits": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProviderSearchHit"
            }
          },
          "provider": {
            "type": "string"
          }
        }
      },
      "RecomputeSpansResponse": {
        "type": "object",
        "description": "Response from `POST /api/v1/series/recompute-spans`. The endpoint runs\nsynchronously, so the counts are the actual outcome of the pass.",
        "required": [
          "releasesRewritten",
          "seriesUpdated"
        ],
        "properties": {
          "releasesRewritten": {
            "type": "integer",
            "format": "int64",
            "description": "Releases whose stored volume/chapter span was rewritten because\nre-parsing the file list (or title) produced a different value.",
            "minimum": 0
          },
          "seriesUpdated": {
            "type": "integer",
            "format": "int64",
            "description": "Series rows whose `highest_volume` / `highest_chapter` changed.",
            "minimum": 0
          }
        }
      },
      "ReenrichRequest": {
        "type": "object",
        "required": [
          "statuses"
        ],
        "properties": {
          "statuses": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Resolution statuses to target. Releases for the source whose\n`resolutionStatus` is in this set have their detail page re-fetched\nand their source-derived columns refreshed; resolution state is left\nuntouched. Must be non-empty and a subset of the known statuses."
          }
        }
      },
      "RefreshAllResponse": {
        "type": "object",
        "required": [
          "results"
        ],
        "properties": {
          "results": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RefreshResponse"
            }
          }
        }
      },
      "RefreshAllSeriesResponse": {
        "type": "object",
        "description": "Response from `POST /api/v1/series/refresh-all`. Mirrors the\ntriggered/skipped pattern used by the source and provider trigger\nendpoints. `batchSize` echoes the config value the spawned tick will\nuse, so the operator can see at a glance how much work was queued.",
        "required": [
          "provider",
          "triggered",
          "skipped",
          "batchSize",
          "minAgeDays"
        ],
        "properties": {
          "batchSize": {
            "type": "integer",
            "format": "int32",
            "description": "Maximum number of series rows this tick will touch, copied from\n`metadata.series_refresh.batch_size`. Reported even on\n`skipped: true` so the UI can render consistent metadata.",
            "minimum": 0
          },
          "minAgeDays": {
            "type": "integer",
            "format": "int32",
            "description": "Minimum age in days a row must have before it's eligible. Echoes\n`metadata.series_refresh.min_age_days`.",
            "minimum": 0
          },
          "provider": {
            "type": "string",
            "description": "Active provider id the refresh ran against."
          },
          "skipped": {
            "type": "boolean",
            "description": "`true` when a previous refresh tick is still in flight; the\nrequest is a no-op."
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "RefreshResponse": {
        "type": "object",
        "required": [
          "provider",
          "triggered",
          "skipped"
        ],
        "properties": {
          "provider": {
            "type": "string"
          },
          "skipped": {
            "type": "boolean",
            "description": "`false` when a refresh is already in flight; the request is a no-op."
          },
          "triggered": {
            "type": "boolean"
          }
        }
      },
      "ReleaseCounts": {
        "type": "object",
        "required": [
          "resolved",
          "unresolved",
          "ambiguous",
          "reviewPending",
          "rejected"
        ],
        "properties": {
          "ambiguous": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "rejected": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "resolved": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "reviewPending": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "unresolved": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "ReleaseDto": {
        "type": "object",
        "required": [
          "id",
          "sourceKind",
          "sourceName",
          "externalId",
          "title",
          "link",
          "files",
          "formats",
          "postedAt",
          "observedAt",
          "resolutionStatus",
          "resolutionAttempts"
        ],
        "properties": {
          "commentSuggestedLinks": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ExtractedLinksDto",
                "description": "Provider links found in the post's *comments* — untrusted, so they are\nnever auto-resolved. The review UI offers them as one-click lookups the\noperator can confirm. Omitted when absent. Distinct from\n`extracted_links` (the uploader's links)."
              }
            ]
          },
          "ddlUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "descriptionHtml": {
            "type": [
              "string",
              "null"
            ],
            "description": "Raw description blob (markdown for Nyaa posts that ran detail fetch;\nthe RSS anchor stub otherwise). Omitted when absent. Surfaced so the\nreview and kept views can render it inline without opening the post."
          },
          "externalId": {
            "type": "string"
          },
          "extractedLinks": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ExtractedLinksDto",
                "description": "External-provider links scraped from the description. Omitted when\nnothing was found."
              }
            ]
          },
          "files": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "formats": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "id": {
            "type": "string"
          },
          "infoHash": {
            "type": [
              "string",
              "null"
            ]
          },
          "informationUrl": {
            "type": [
              "string",
              "null"
            ],
            "description": "URL from the post's \"Information\" field, verbatim. Surfaced even when\nit is not a provider link we resolve against (a publisher page, a\nDiscord invite, …) so the review and kept views show the uploader's\ncited source. Omitted when absent."
          },
          "lastResolveAttemptAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "link": {
            "type": "string"
          },
          "magnet": {
            "type": [
              "string",
              "null"
            ]
          },
          "observedAt": {
            "type": "integer",
            "format": "int64"
          },
          "postedAt": {
            "type": "integer",
            "format": "int64"
          },
          "resolutionAttempts": {
            "type": "integer",
            "format": "int32"
          },
          "resolutionConfidence": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "resolutionPath": {
            "type": [
              "string",
              "null"
            ]
          },
          "resolutionStatus": {
            "type": "string"
          },
          "seriesId": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          },
          "sizeBytes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "sourceKind": {
            "type": "string"
          },
          "sourceName": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "torrentUrl": {
            "type": [
              "string",
              "null"
            ]
          }
        }
      },
      "ReleaseGroupCandidateDto": {
        "type": "object",
        "description": "Display-only top-candidate hint attached to a [`ReleaseGroupDto`].",
        "required": [
          "seriesId",
          "title"
        ],
        "properties": {
          "coverUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "seriesId": {
            "type": "integer",
            "format": "int32"
          },
          "title": {
            "type": "string"
          }
        }
      },
      "ReleaseGroupDto": {
        "type": "object",
        "description": "A cluster of review-queue releases sharing one cleaned search query. The\n`query` is exactly the value to hand back as `searchQuery` (at the same\n`breadth`) to filter the list down to this group's members.",
        "required": [
          "query",
          "count"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "format": "int64",
            "description": "Number of distinct releases in the group (always > 1)."
          },
          "query": {
            "type": "string",
            "description": "The shared cleaned search query, and the `searchQuery` filter value\nthat reproduces this group."
          },
          "topCandidate": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/ReleaseGroupCandidateDto",
                "description": "Optional display-only hint about the group's most common top-candidate\nseries. Populated in a later phase; currently always absent. Never a\ngrouping key."
              }
            ]
          }
        }
      },
      "ReleaseGroupsResponse": {
        "type": "object",
        "description": "Response for the grouped review-queue endpoint.",
        "required": [
          "breadth",
          "groups"
        ],
        "properties": {
          "breadth": {
            "type": "integer",
            "format": "int32",
            "description": "The breadth the groups were computed at (clamped to {1,2,3}). Echoed so\nthe client can pass it back as the list `breadth` unchanged.",
            "minimum": 0
          },
          "groups": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReleaseGroupDto"
            },
            "description": "Clusters with more than one member, ordered by descending count then\nquery ascending."
          }
        }
      },
      "ReleasePage": {
        "type": "object",
        "required": [
          "items",
          "page",
          "pageSize",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReleaseDto"
            }
          },
          "page": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "pageSize": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "ResolutionOutcomeBreakdown": {
        "type": "object",
        "required": [
          "knownId",
          "foreignId",
          "fuzzy",
          "review",
          "failed"
        ],
        "properties": {
          "failed": {
            "type": "integer",
            "format": "int64"
          },
          "foreignId": {
            "type": "integer",
            "format": "int64"
          },
          "fuzzy": {
            "type": "integer",
            "format": "int64"
          },
          "knownId": {
            "type": "integer",
            "format": "int64"
          },
          "review": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "RetryAllResponse": {
        "type": "object",
        "required": [
          "triggered",
          "skipped"
        ],
        "properties": {
          "skipped": {
            "type": "boolean",
            "description": "`true` when a prior retry-all batch is still in flight; the request is a no-op."
          },
          "triggered": {
            "type": "boolean",
            "description": "`true` when a batch was spawned by this request."
          }
        }
      },
      "ReviewCandidateDto": {
        "type": "object",
        "required": [
          "seriesId",
          "seriesTitle",
          "score",
          "alternateTitles"
        ],
        "properties": {
          "alternateTitles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Alternate / native titles persisted on the series row. Surfaced\nin the review UI so romaji / Japanese / publisher variants are\nvisible without opening the provider page."
          },
          "externalId": {
            "type": [
              "string",
              "null"
            ]
          },
          "kind": {
            "type": [
              "string",
              "null"
            ],
            "description": "Series format (manga / novel / manhwa / …) from provider metadata.\nSurfaced so the operator can sanity-check a candidate's format\nagainst the release before linking. `None` when the provider did\nnot classify the series."
          },
          "provider": {
            "type": [
              "string",
              "null"
            ],
            "description": "Provider + external_id pair for building a link to the provider's\npage. Prefers the active provider's mapping; falls back to any\nother mapping so the operator always has a way to inspect the\ncandidate. None when the series has no external IDs persisted."
          },
          "reason": {
            "type": [
              "string",
              "null"
            ]
          },
          "score": {
            "type": "number",
            "format": "double"
          },
          "seriesCoverUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "seriesId": {
            "type": "integer",
            "format": "int32"
          },
          "seriesTitle": {
            "type": "string"
          },
          "totalChapters": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          },
          "totalVolumes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Published volume/chapter counts from provider metadata. Let the\noperator compare the release's contents (see the torrent file list)\nagainst the candidate series' length. `None` when the provider did\nnot expose them or the series predates this field."
          }
        }
      },
      "ReviewQueueMetrics": {
        "type": "object",
        "required": [
          "snapshots",
          "closedCount",
          "rangeSeconds",
          "since",
          "until"
        ],
        "properties": {
          "closedCount": {
            "type": "integer",
            "format": "int64"
          },
          "rangeSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "since": {
            "type": "integer",
            "format": "int64"
          },
          "snapshots": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReviewQueueSnapshotDto"
            }
          },
          "timeToDecisionP50Seconds": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Median time (seconds) from observed to resolved, over closures in\n`[since, until)`. `null` when nothing closed."
          },
          "until": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "ReviewQueueSnapshotDto": {
        "type": "object",
        "required": [
          "capturedAt",
          "pendingCount",
          "unresolvedCount",
          "ambiguousCount",
          "reviewPendingCount"
        ],
        "properties": {
          "ambiguousCount": {
            "type": "integer",
            "format": "int64"
          },
          "capturedAt": {
            "type": "integer",
            "format": "int64"
          },
          "oldestPendingSeconds": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "pendingCount": {
            "type": "integer",
            "format": "int64"
          },
          "reviewPendingCount": {
            "type": "integer",
            "format": "int64"
          },
          "unresolvedCount": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SeriesDetail": {
        "type": "object",
        "required": [
          "id",
          "canonicalTitle",
          "alternateTitles",
          "genres",
          "tags",
          "metadataSource",
          "metadataFetchedAt",
          "firstSeenAt",
          "lastReleaseAt",
          "owned",
          "externalIds"
        ],
        "properties": {
          "alternateTitles": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "canonicalTitle": {
            "type": "string"
          },
          "codex": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CodexInfo",
                "description": "Codex presence overlay. Admin-only; absent for non-admins and for\nseries not on Codex."
              }
            ]
          },
          "coverUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ]
          },
          "externalIds": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExternalIdDto"
            }
          },
          "firstSeenAt": {
            "type": "integer",
            "format": "int64"
          },
          "genres": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "highestChapter": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "highestVolume": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "id": {
            "type": "integer",
            "format": "int32"
          },
          "kind": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastReleaseAt": {
            "type": "integer",
            "format": "int64"
          },
          "metadataFetchedAt": {
            "type": "integer",
            "format": "int64"
          },
          "metadataSource": {
            "type": "string"
          },
          "owned": {
            "type": "boolean",
            "description": "Whether the operator owns this series on Codex. Derived from the\npresence of [`Self::codex`]; only ever `true` for admins."
          },
          "rating": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Provider rating on a 0-10 scale (normalized in the provider's\nmapping layer). `None` when the provider has no rating."
          },
          "status": {
            "type": [
              "string",
              "null"
            ]
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "totalChapters": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Published total chapter count from provider metadata. See\n[`Self::total_volumes`] for how this differs from `highest_chapter`."
          },
          "totalVolumes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Published total volume count from provider metadata. Distinct from\n[`Self::highest_volume`], which tracks the highest span observed\nacross releases. `None` if the provider has no value for this row."
          },
          "year": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          }
        }
      },
      "SeriesListItem": {
        "type": "object",
        "required": [
          "id",
          "canonicalTitle",
          "genres",
          "tags",
          "metadataSource",
          "lastReleaseAt",
          "firstSeenAt",
          "releaseCount",
          "owned"
        ],
        "properties": {
          "canonicalTitle": {
            "type": "string"
          },
          "codex": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/CodexInfo",
                "description": "Codex presence overlay. Present **only** for admin-authenticated\nrequests and **only** when the series is on Codex; the key is absent\n(not null) otherwise, so the public read tier never learns library\ncontents."
              }
            ]
          },
          "coverUrl": {
            "type": [
              "string",
              "null"
            ]
          },
          "description": {
            "type": [
              "string",
              "null"
            ],
            "description": "Short synopsis. The list UI clamps this to a few lines; the detail\npage shows it in full."
          },
          "firstSeenAt": {
            "type": "integer",
            "format": "int64"
          },
          "genres": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "highestChapter": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Highest chapter number seen across linked releases. Pairs with\n[`Self::total_chapters`] for the `ch available/total` card badge."
          },
          "highestVolume": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Highest volume number seen across this series' linked releases (what\nis realistically available), as opposed to [`Self::total_volumes`]\n(the published total). Drives the `vol available/total` card badge\nand the `highest_volume` sort. `None` until a numbered release links."
          },
          "id": {
            "type": "integer",
            "format": "int32"
          },
          "kind": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastReleaseAt": {
            "type": "integer",
            "format": "int64"
          },
          "metadataSource": {
            "type": "string",
            "description": "Provenance of the row's metadata (`offline_cache`, `api`, or `manual`).\nThe browse UI flags `manual` series so they read differently from\nprovider-backed ones (no cover/metadata is expected)."
          },
          "owned": {
            "type": "boolean",
            "description": "Whether the operator owns this series on Codex. Derived from the\npresence of [`Self::codex`], so it is only ever `true` for admins."
          },
          "rating": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Provider rating on a 0-10 scale; surfaced on the list view so a\nfuture sort-by-rating has a number to display."
          },
          "releaseCount": {
            "type": "integer",
            "format": "int64",
            "description": "Number of releases currently linked to this series. Surfaced as a\nbadge in the feed so manual re-links that orphan a series (zero\nreleases) are visible at a glance. Matches what\n`GET /releases?seriesId=…` returns."
          },
          "status": {
            "type": [
              "string",
              "null"
            ]
          },
          "tags": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "totalChapters": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Published total chapter count from provider metadata; pair to\n[`Self::total_volumes`] for sort-by-chapter results."
          },
          "totalVolumes": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32",
            "description": "Published total volume count from provider metadata; surfaced on\nthe list view so sort-by-volume results have a number to display."
          },
          "year": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int32"
          }
        }
      },
      "SeriesListPage": {
        "type": "object",
        "required": [
          "items",
          "page",
          "pageSize",
          "total"
        ],
        "properties": {
          "codexSyncedAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Timestamp of the last successful Codex sweep, or `null`/absent when not\nadmin or no sweep has succeeded. The UI suppresses all Codex badges\nwhile this is absent so a pre-first-sync admin doesn't see false\n\"not owned\" states. Admin-only."
          },
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SeriesListItem"
            }
          },
          "page": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "pageSize": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "SourceConfigDto": {
        "type": "object",
        "description": "Operator-facing snapshot of the per-source config (the bits visible in\n`[[sources]]` and any kind-specific nested options). Never carries\nsecrets; the source layer doesn't have any in v1.",
        "required": [
          "enabled",
          "feedUrl",
          "fetchDetails",
          "timeoutSeconds",
          "maxPages"
        ],
        "properties": {
          "cron": {
            "type": [
              "string",
              "null"
            ]
          },
          "enabled": {
            "type": "boolean"
          },
          "feedUrl": {
            "type": "string",
            "description": "Feed URL for the source kind. Empty when the kind doesn't define one\n(every kind today does, so this is informational future-proofing)."
          },
          "fetchDetails": {
            "type": "boolean"
          },
          "maxPages": {
            "type": "integer",
            "format": "int32",
            "description": "Maximum number of feed pages walked per poll. Always `1` today:\nno v1 source kind paginates inside the steady-state poll. Kept on\nthe DTO as a placeholder for future paginated source kinds; the\n`backfill` CLI is the historical-catch-up path in the meantime.",
            "minimum": 0
          },
          "siteBaseUrl": {
            "type": [
              "string",
              "null"
            ],
            "description": "Override for the site base URL. Useful when the feed is proxied."
          },
          "timeoutSeconds": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          }
        }
      },
      "SourceDto": {
        "type": "object",
        "required": [
          "name",
          "kind"
        ],
        "properties": {
          "config": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SourceConfigDto",
                "description": "Static config block snapshotted at boot. `None` when a source is in\nthe registry but its config entry isn't, which today only happens in\ntests using a hand-built `SourceRegistry`."
              }
            ]
          },
          "inFlight": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/InFlight",
                "description": "Set when a `poll_runs` row for this source is currently in\n`status = 'running'`. Lets the admin UI render the \"RUNNING…\" pill\nfrom a fresh page load instead of waiting for an SSE event the\nchannel doesn't replay."
              }
            ]
          },
          "kind": {
            "type": "string"
          },
          "lastError": {
            "type": [
              "string",
              "null"
            ]
          },
          "lastPolledAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "lastSuccessAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "lastSummary": {
            "type": [
              "string",
              "null"
            ]
          },
          "name": {
            "type": "string"
          }
        }
      },
      "SourceList": {
        "type": "object",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SourceDto"
            }
          }
        }
      },
      "SourceMetricsBucket": {
        "type": "object",
        "required": [
          "bucketStart",
          "successCount",
          "failureCount",
          "skippedCount"
        ],
        "properties": {
          "bucketStart": {
            "type": "integer",
            "format": "int64"
          },
          "failureCount": {
            "type": "integer",
            "format": "int64"
          },
          "fetchedSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "newSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "skippedCount": {
            "type": "integer",
            "format": "int64"
          },
          "successCount": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SourceMetricsDetail": {
        "type": "object",
        "required": [
          "sourceName",
          "buckets",
          "errorKinds",
          "fetchLatency",
          "timeToResolution",
          "bucketSeconds",
          "rangeSeconds",
          "since",
          "until"
        ],
        "properties": {
          "bucketSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "buckets": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SourceMetricsBucket"
            }
          },
          "errorKinds": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ErrorKindBucket"
            }
          },
          "fetchLatency": {
            "$ref": "#/components/schemas/FetchLatencyDto"
          },
          "rangeSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "since": {
            "type": "integer",
            "format": "int64"
          },
          "sourceName": {
            "type": "string"
          },
          "summary": {
            "oneOf": [
              {
                "type": "null"
              },
              {
                "$ref": "#/components/schemas/SourceMetricsSummaryItem"
              }
            ]
          },
          "timeToResolution": {
            "$ref": "#/components/schemas/TimeToResolutionDto"
          },
          "until": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SourceMetricsSummary": {
        "type": "object",
        "required": [
          "items",
          "rangeSeconds",
          "since",
          "until"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SourceMetricsSummaryItem"
            }
          },
          "rangeSeconds": {
            "type": "integer",
            "format": "int64"
          },
          "since": {
            "type": "integer",
            "format": "int64"
          },
          "until": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "SourceMetricsSummaryItem": {
        "type": "object",
        "required": [
          "sourceName",
          "totalRuns",
          "successCount",
          "failureCount",
          "skippedCount",
          "outcomes"
        ],
        "properties": {
          "enrichDurationMsSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Total milliseconds spent in `DiscoverySource::enrich()` across every\nrun in the window. `null` when no runs reported it."
          },
          "failureCount": {
            "type": "integer",
            "format": "int64"
          },
          "fetchedSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "lastStartedAt": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "lastStatus": {
            "type": [
              "string",
              "null"
            ]
          },
          "newSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "outcomes": {
            "$ref": "#/components/schemas/ResolutionOutcomeBreakdown"
          },
          "resolveDurationMsSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64",
            "description": "Total milliseconds spent in `Resolver::resolve_one()` across every\nrun in the window. `null` when no runs reported it."
          },
          "resolvedSum": {
            "type": [
              "integer",
              "null"
            ],
            "format": "int64"
          },
          "skippedCount": {
            "type": "integer",
            "format": "int64"
          },
          "sourceName": {
            "type": "string"
          },
          "successCount": {
            "type": "integer",
            "format": "int64"
          },
          "successRate": {
            "type": [
              "number",
              "null"
            ],
            "format": "double",
            "description": "Convenience derivation: `successCount / (successCount + failureCount)`,\n`null` when the denominator is zero. Saves the client from a no-op\ndivision when nothing has run yet."
          },
          "totalRuns": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "StatsResponse": {
        "type": "object",
        "required": [
          "series",
          "releases",
          "totalReleases",
          "activeProvider"
        ],
        "properties": {
          "activeProvider": {
            "type": "string"
          },
          "releases": {
            "$ref": "#/components/schemas/ReleaseCounts"
          },
          "series": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          },
          "totalReleases": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "TagList": {
        "type": "object",
        "required": [
          "items"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TagUsageDto"
            }
          }
        }
      },
      "TagUsageDto": {
        "type": "object",
        "required": [
          "name",
          "seriesCount"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "seriesCount": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "TimeToResolutionDto": {
        "type": "object",
        "required": [
          "count"
        ],
        "properties": {
          "count": {
            "type": "integer",
            "format": "int64"
          },
          "p50Seconds": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          },
          "p95Seconds": {
            "type": [
              "number",
              "null"
            ],
            "format": "double"
          }
        }
      },
      "UnresolvedPage": {
        "type": "object",
        "required": [
          "items",
          "page",
          "pageSize",
          "total"
        ],
        "properties": {
          "items": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/UnresolvedRelease"
            }
          },
          "page": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "pageSize": {
            "type": "integer",
            "format": "int32",
            "minimum": 0
          },
          "total": {
            "type": "integer",
            "format": "int64",
            "minimum": 0
          }
        }
      },
      "UnresolvedRelease": {
        "allOf": [
          {
            "$ref": "#/components/schemas/ReleaseDto"
          },
          {
            "type": "object",
            "required": [
              "candidates",
              "searchQueries",
              "cleanupRulesApplied"
            ],
            "properties": {
              "candidates": {
                "type": "array",
                "items": {
                  "$ref": "#/components/schemas/ReviewCandidateDto"
                }
              },
              "cleanupRulesApplied": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Stable rule names that fired during cleanup (e.g. `strip_parens`,\n`split_alternates`). Rendered as badge chips on the review card."
              },
              "searchQueries": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Search queries the title cleaner produced for this release\n(longest-first). Empty when the release predates the cleaner or\nfailed to persist; the next resolve cycle backfills."
              },
              "topCandidate": {
                "oneOf": [
                  {
                    "type": "null"
                  },
                  {
                    "$ref": "#/components/schemas/ReviewCandidateDto",
                    "description": "Convenience pointer to `candidates[0]`, when present. Lets the\ncard render without defensive-checking the array on every render."
                  }
                ]
              }
            }
          }
        ]
      }
    },
    "securitySchemes": {
      "admin": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "opaque",
        "description": "Admin bearer token (auth.admin_token in config)."
      }
    }
  },
  "tags": [
    {
      "name": "system",
      "description": "Health and aggregate counters"
    },
    {
      "name": "series",
      "description": "Resolved series catalog"
    },
    {
      "name": "releases",
      "description": "Raw release feed and review queue"
    },
    {
      "name": "sources",
      "description": "Discovery-source state and triggers"
    },
    {
      "name": "providers",
      "description": "Metadata-provider state and triggers"
    },
    {
      "name": "tagging",
      "description": "Canonical genre and tag lists for filter UI"
    },
    {
      "name": "metrics",
      "description": "Per-source / per-provider historical run metrics"
    },
    {
      "name": "covers",
      "description": "Cover-image proxy and on-disk cache control"
    },
    {
      "name": "codex",
      "description": "Codex presence integration (admin-only)"
    }
  ]
}